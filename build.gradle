plugins {
    id "com.gradle.plugin-publish" version "0.9.1"
}

apply from: 'plugin.gradle'
apply plugin: 'groovy'

version = "0.3"
group = "com.trevorism"

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    testCompile 'junit:junit:4.12'
}
